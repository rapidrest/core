# rapidrest/core

Before doing a code review or security review of `src/`, read `.claude/review-notes.md` first.
It records design decisions this project has already settled (things that look like bugs but
aren't, given this library's actual usage contract) and issues already found and fixed across
prior review passes, so reviews don't re-spend effort on already-closed ground.
